import React from 'react';
import './Message.css';

export default function Message() {
  return (
    <p className='message'>
      Resize the browser window slightly if the apps look stretched vertically.
      This can happen when first loading the page.
    </p>
  );
}
